import { Typography, Box } from "@mui/material";

export const NotVerified = () => {
  return (
    <Box sx={{ textAlign: 'center', mt:20, mb:20}}>
        <Typography variant="h4" >Debe verificar su cuenta con el mail que le llega para poder continuar con sus datos personales</Typography>
    </Box>
  )
}
